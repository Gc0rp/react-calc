import React from 'react';

const previousCalculationsStyle = {
    textAlign: 'right',
    padding: '20px 20px 0px 0px',
};

const PreviousCalculations = () => (
    <div style={previousCalculationsStyle}>
        <h4 style={{fontFamily: 'sans-serif', fontWeight: '400', fontSize: '20px', color: "white"}}>1395 + 0.5</h4>
    </div>
);

export default PreviousCalculations;